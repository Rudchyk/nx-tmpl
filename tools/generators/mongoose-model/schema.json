{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "mongooseModel",
  "title": "Nx Mongoose Model Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Mongoose Model name",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
